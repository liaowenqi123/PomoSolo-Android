[安卓端部门] feat: P2P 打洞测试工具（与桌面端互通，可 Android↔桌面 互测）

协议依据（主仓库）：server-planning/ws_server.py 的 handle_p2p_* （p2p:online / test_request /
test_result / reverse_test_request / bidir_test_request，服务端只做定向转发 + 加 from_user_id）、
桌面端 src/api/p2pTest.ts、src-tauri/src/commands/p2p.rs、src/components/P2PTestPanel.vue、src/p2p.ts。

改造 data/P2PTransfer.kt（支持多连接与双向测速）：
- 会话按 `peerId:tag` 路由（对齐桌面端路由键），支持同一对端多条并发连接；
- 信令 peer:offer/answer/ice 均带 `tag` 原样透传；收到 offer 时优先复用已挂起接收会话；
- 实现 duplex-test 时序（与桌面端一致）：offerer 推完发 `duplex_switch` 等 `duplex_done`；
  answerer 收到 switch 后推第二程、推完发 `duplex_done` 并结束；
- 数据源可注入（真实文件）或随机数据（测速），64KB 分片 + 4 字节大端序号 + bufferedAmount 背压；
- 结果按桌面端字段返回：ok / ms / speed_bps / bytes / error。

新增 data/P2PTestStore.kt（测试业务）：
- refreshUsers()：`p2p:online`（带 id 请求-响应，8s 超时）→ 在线用户列表（camelCase userId/username）；
- runFullTest(user)：A（`p2p:test_request` tag=a，本机 offerer）→ B（`p2p:reverse_test_request`
  tag=b，本机挂起作 answerer）两轮，逐轮刷新结果；
- 被动应答：收到 p2p:test_request / reverse_test_request / bidir_test_request 自动建连接收或发送，
  完成后 `p2p:test_result` 回传；收到对端结果时在界面提示（双方视角可见）。

新增 ui/P2PTestScreen.kt + 设置页入口（设置 → 调试 → P2P 打洞测试）：
- 在线用户列表（点用户开始测试）、实时进度（已传 KB / Mbps / 已直连或打洞中）、
  结果卡片（↑ 上行 / ↓ 下行 / 详情）、提示条（固定高度、6s 自动清除）。
- 该页无需进自习室，只要双方登录并保持 WS 在线即可互测。

其它：StudyRoomStore 新增 request()（请求-响应发送）并把 p2p:* 分发给 P2PTestStore；
README 的 m5.1 小节补充测试工具说明。

构建：BUILD SUCCESSFUL；MuMu（Android 15 / x86_64）安装运行正常，设置页入口与 AI 选片配置显示正常
（顺带确认上一轮 Switch 关闭态对比度修正生效）。
